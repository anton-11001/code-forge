import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#8b5e34",
    },
    success: {
      main: "#2e7d32",
    },
    background: {
      default: "#f2ebe3",
      paper: "#fffdf9",
    },
  },
});

export default theme;
